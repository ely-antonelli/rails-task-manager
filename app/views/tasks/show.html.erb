<div>
<div>
<h1 class="header"><%= @task.name %></h1>
 <%= link_to "Edit this task", edit_task_path(@task), class: "button-edit btn-grey" %>
</div>
<%= @task.details %>
<%= content_tag :div, @message, class: "status" %>
<%= link_to "Back to the list", tasks_path %>
<%= link_to "Delete", task_path(@task), class: "container-delete",
 data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</div>
</div>
